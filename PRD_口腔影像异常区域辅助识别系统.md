# 口腔影像异常区域辅助识别系统 PRD

## 1. 项目背景
本系统面向口腔影像异常区域识别场景，基于当前训练效果较好的 `exp6` 模型作为后端识别核心，提供一个可交互的前后端应用。

系统核心目标不是直接替代人工判断，而是提供“模型初步识别 + 用户交互修正 + 差异量化展示”的辅助诊断能力。

---

## 2. 产品目标

### 2.1 总体目标
构建一个支持上传口腔照片、自动识别异常大致区域、支持用户手动修正区域、并计算“模型识别区域与用户修正区域差异比例”的 Web 应用。

### 2.2 目标用户
- 毕业设计演示使用者
- 教师答辩查看者
- 后续可能扩展的口腔影像辅助标注人员

### 2.3 产品价值
- 展示深度学习模型在口腔影像识别中的应用能力
- 展示人机协同场景，而非纯模型黑盒输出
- 支持对模型结果进行交互式修正
- 将“模型结果”和“人工修正结果”进行量化对比，体现系统分析能力

---

## 3. 产品定位
本产品定位为：

**基于 YOLOv8(exp6) 的口腔影像异常区域辅助识别与交互修正系统**。

特点：
- 前端支持图片上传与结果可视化
- 后端调用 `exp6` 模型完成异常区域初检
- 支持用户对检测框进行拖动和调整
- 支持输出修正前后区域差异比例

---

## 4. 使用场景

### 场景 1：普通识别
用户上传一张口腔照片，系统自动识别异常区域，并在图片上框出候选区域。

### 场景 2：人工修正
如果模型框的位置不够准确，用户可以手动拖动检测框，调整到认为更合理的位置。

### 场景 3：差异分析
用户调整完成后，系统实时显示：
- 原始模型框坐标
- 用户修正后框坐标
- 两者面积差异
- 两者重叠比例 / 差异比例

### 场景 4：毕业设计演示
演示“上传图片 -> 模型检测 -> 手动修正 -> 差异量化”的完整流程，突出模型能力与交互性。

---

## 5. 产品范围

### 5.1 本期范围（V1）
本期只做最小可用闭环：
- 单张图片上传
- 后端调用 `exp6` 模型推理
- 返回异常区域框
- 前端展示原图和检测框
- 用户拖动 / 调整检测框
- 展示修正前后差异比例

### 5.2 暂不纳入范围
以下功能不在第一版中实现：
- 用户登录注册
- 历史记录管理
- 多人协作标注
- 多目标复杂编辑
- 云端部署
- 医疗报告自动生成
- 真正意义上的“标准答案框”数据库比对

---

## 6. 功能需求

## 6.1 前端功能需求

### 6.1.1 图片上传
用户可通过页面上传本地图片。

#### 输入方式
- 点击上传
- 拖拽上传（可选）

#### 限制要求
- 支持格式：`jpg`、`jpeg`、`png`
- 单张图片大小限制：建议不超过 10MB

#### 页面反馈
- 上传成功后显示原图预览
- 上传失败时提示错误原因

---

### 6.1.2 模型识别结果展示
上传图片后，前端调用后端识别接口。

#### 展示内容
- 原始图片
- 模型返回的检测框
- 检测置信度（可选显示）
- 检测框坐标信息（可选显示）

#### 交互要求
- 页面加载后，自动将识别框绘制到图片上
- 若检测到多个框，默认展示全部
- 后续 V1 可先只处理“置信度最高的一个框”

---

### 6.1.3 检测框手动调整
用户可对模型返回的框进行人工修正。

#### 支持操作
- 拖动框整体位置
- 拖动四角或边缘调整大小
- 重置为模型原始框

#### 交互要求
- 用户操作后，页面应实时刷新框的位置和尺寸
- 保留“原始框”和“修正框”两种状态
- 原始框与修正框建议用不同颜色区分

建议颜色：
- 原始框：红色
- 修正框：绿色

---

### 6.1.4 数值展示专区
页面需有一个专门的数值展示区域，用于展示模型框与修正框之间的对比结果。

#### 展示内容
1. 模型原始框坐标
   - `x`
   - `y`
   - `width`
   - `height`

2. 用户修正框坐标
   - `x`
   - `y`
   - `width`
   - `height`

3. 对比指标
   - 原始框面积
   - 修正框面积
   - 交并比 IoU
   - 差异比例

#### 差异比例定义建议
差异比例可定义为：

\[
差异比例 = 1 - IoU
\]

其中：

\[
IoU = \frac{原始框与修正框的交集面积}{原始框与修正框的并集面积}
\]

解释：
- IoU 越高，说明模型框与修正框越接近
- 差异比例越低，说明模型预测越接近人工修正结果

#### 页面展示建议
- IoU：例如 `0.78`
- 差异比例：例如 `22%`
- 可用进度条或醒目标识展示

---

### 6.1.5 结果状态提示
系统需要给用户明确状态反馈：
- 上传中
- 识别中
- 识别成功
- 未检测到目标
- 调整已完成

---

## 6.2 后端功能需求

### 6.2.1 图片接收接口
后端提供图片上传与识别接口。

#### 基本要求
- 接收前端上传的单张图片
- 完成图片保存或内存处理
- 调用 `exp6` 模型进行推理
- 返回预测框数据

---

### 6.2.2 模型推理模块
后端使用当前最优实验模型 `exp6` 的权重进行推理。

#### 模型要求
- 模型：YOLOv8 + CBAM（exp6）
- 输入：用户上传的口腔图像
- 输出：检测框信息

#### 返回字段建议
- `bbox`: `[x1, y1, x2, y2]`
- `confidence`
- `class_id`
- `class_name`
- `image_width`
- `image_height`

---

### 6.2.3 修正框差异计算接口
当前端用户完成拖动后，将修正框传给后端，后端负责计算差异指标并返回。

#### 输入
- 模型原始框
- 用户修正框

#### 输出
- 原始框面积
- 修正框面积
- 交集面积
- 并集面积
- IoU
- 差异比例

#### 备注
该功能也可以前端本地计算，但为了保证逻辑统一，建议由后端提供标准计算接口。

---

## 7. 页面结构设计

## 7.1 页面总体布局
建议采用三栏或上下分区布局。

### 方案 A：左右布局
- 左侧：图片上传区 + 原图展示
- 中间：识别结果与可拖拽标注区
- 右侧：数值展示区

### 方案 B：上下布局
- 上方：上传区 + 按钮区
- 中间：图片识别与标注画布
- 下方：数值信息区

### 推荐
优先采用 **左右布局**，更适合答辩演示，视觉更直观。

---

## 7.2 页面模块划分

### 模块 1：上传区
元素：
- 上传按钮
- 图片预览
- 开始识别按钮（若不自动识别）

### 模块 2：图像标注区
元素：
- 原图
- 模型原始框
- 用户修正框
- 框选拖动控制点

### 模块 3：结果分析区
元素：
- 模型结果信息
- 用户修正信息
- IoU
- 差异比例
- 状态提示

---

## 8. 业务流程

## 8.1 主流程
1. 用户进入页面
2. 用户上传一张口腔图片
3. 前端将图片发送到后端识别接口
4. 后端调用 `exp6` 模型进行推理
5. 后端返回检测框结果
6. 前端在图片上绘制模型识别框
7. 用户手动拖动 / 缩放识别框
8. 前端将修正框发送到差异计算接口，或本地计算
9. 页面展示 IoU 与差异比例
10. 用户查看最终结果

---

## 9. 接口设计建议

## 9.1 图片识别接口
### 接口路径
`POST /api/detect`

### 请求方式
`multipart/form-data`

### 请求参数
- `file`: 图片文件

### 返回示例
```json
{
  "success": true,
  "image": {
    "width": 1024,
    "height": 768,
    "url": "/uploads/xxx.jpg"
  },
  "detections": [
    {
      "bbox": [120, 80, 320, 260],
      "confidence": 0.91,
      "class_id": 0,
      "class_name": "abnormal"
    }
  ]
}
```

---

## 9.2 差异计算接口
### 接口路径
`POST /api/compare`

### 请求示例
```json
{
  "original_bbox": [120, 80, 320, 260],
  "edited_bbox": [130, 90, 310, 250]
}
```

### 返回示例
```json
{
  "success": true,
  "original_area": 36000,
  "edited_area": 28800,
  "intersection_area": 25200,
  "union_area": 39600,
  "iou": 0.6364,
  "difference_ratio": 0.3636
}
```

---

## 10. 非功能需求

### 10.1 性能要求
- 单张图片识别时间尽量控制在 3 秒以内
- 前端拖动框时应实时响应，无明显卡顿

### 10.2 可用性要求
- 页面操作简单，适合演示
- 提示信息明确
- 异常状态清晰

### 10.3 可维护性要求
- 前后端解耦
- 模型路径可配置
- 后续可替换为其他实验权重

---

## 11. 技术方案建议

## 11.1 前端建议
可选技术：
- `Vue 3 + Element Plus`
- 或 `React + Ant Design`

建议能力：
- 图片上传
- Canvas / SVG 绘制框
- 拖拽交互
- 实时数值更新

### 前端关键点
- 图片坐标和原图坐标的换算
- 拖拽缩放框的交互体验
- 原始框、修正框同时显示

---

## 11.2 后端建议
可选技术：
- `FastAPI`
- 或 `Flask`

建议优先：`FastAPI`

原因：
- 轻量
- 接口清晰
- 文档自动生成方便
- 和 Python 模型推理集成自然

---

## 11.3 模型推理方案
- 启动服务时加载 `exp6` 模型权重
- 接收到图片后直接推理
- 返回 bbox 和置信度

建议将模型封装为独立服务模块，例如：
- `services/detector.py`
- `api/routes/detect.py`

---

## 12. 数据结构建议

### 12.1 检测框对象
```json
{
  "x1": 120,
  "y1": 80,
  "x2": 320,
  "y2": 260,
  "confidence": 0.91
}
```

### 12.2 编辑框对象
```json
{
  "x1": 130,
  "y1": 90,
  "x2": 310,
  "y2": 250
}
```

---

## 13. 验收标准

### 13.1 功能验收
- 可以成功上传图片
- 可以成功调用后端识别接口
- 页面可以显示模型识别框
- 用户可以拖动和调整框位置
- 页面可以展示修正前后差异比例

### 13.2 演示验收
- 演示流程完整
- 页面清晰、美观
- 数据反馈直观
- 支持答辩时现场操作

---

## 14. 风险与注意事项

### 风险 1：模型识别不稳定
解决建议：
- V1 优先展示置信度最高的框
- 对检测不到目标的情况做空状态提示

### 风险 2：前端坐标和原图坐标不一致
解决建议：
- 明确记录图片显示缩放比例
- 所有框计算统一使用原图坐标系

### 风险 3：拖拽实现复杂
解决建议：
- 优先用成熟画布库或现成拖拽方案
- V1 可先只支持单框编辑

---

## 15. 版本规划

### V1
- 单图上传
- 模型识别
- 单框编辑
- 差异比例展示

### V2
- 多框编辑
- 历史记录
- 导出结果
- 对比更多模型结果

---

## 16. 当前建议的实现优先级

### P0
- 后端加载 `exp6` 模型
- 图片上传接口
- 检测结果返回
- 前端图片展示
- 检测框绘制
- 单框拖动编辑
- IoU / 差异比例展示

### P1
- 原图 / 修正图切换
- 多框支持
- 置信度展示优化
- 结果导出

---

## 17. 一句话总结
本系统是一个基于 `exp6` 模型的口腔影像异常区域辅助识别 Web 应用，能够实现“上传图片 -> 模型检测 -> 人工修正 -> 差异量化展示”的完整闭环，突出模型能力与人机交互价值。
